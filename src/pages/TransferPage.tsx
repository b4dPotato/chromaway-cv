import { Box, Typography } from '@mui/material';

const TransferPage = () => {
  return (
    <Box>
      <Typography variant="h1">Transfer Page</Typography>
    </Box>
  );
};

export default TransferPage;
