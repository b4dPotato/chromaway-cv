import { Box, Typography } from '@mui/material';

const WalletPage = () => {
  return (
    <Box>
      <Typography variant="h1">Wallet Page</Typography>
    </Box>
  );
};

export default WalletPage;
